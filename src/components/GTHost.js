import React, { Component, PropTypes } from 'react';

const propTypes = {
};

class GTHost extends Component {
  render() {
    return (
      <div>Host: Eithan</div>
    );
  }
}

GTHost.propTypes = propTypes;
export default GTHost;
